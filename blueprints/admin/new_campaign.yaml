form:
  validation: loose
  fields:

    section:
      type: section
      title: 'Create an email'

    title:
      type: text
      label: 'Campaign Name'
      validate:
        required: true
    folder:
      type: hidden
      default: '@slugify-title'

    name:
      type: hidden
      default: 'campaign'

    route:
      type: hidden
      default: /campaign

    blueprint:
      type: blueprint
